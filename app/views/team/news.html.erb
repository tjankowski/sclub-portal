<%= render :partial => "menu" %>
<div class="box">
  <div class="box_header">
    <h1>
      <%= @team.name %> - Newsy
    </h1>
  </div>
  <div class="box_content">
  <% if @articles.empty? %>
    <h4 class="center_align">
        Niestety nie został jeszcze napisany artykuł pasujący do tej kategorii.
      </h4>
    <% end %>
  <%= render :partial =>"articles/article_short", :collection => @articles %>
  <%= will_paginate @articles, :prev_label => "Poprzednia", :next_label => "Następna" %>
  </div>
</div>