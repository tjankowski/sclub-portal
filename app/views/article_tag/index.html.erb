<% if logged_in? %>
  <% if current_user.has_role?('administrator')  %>
    <div class="box">
      <div class="box_content">
        <%= tool_link 'add.png', 'Nowy tag', 'tags', 'new' %>
      </div>
    </div>
  <% end %>
<% end %>
<div class="box">
  <div class="box_header">
    <h1>
      Tagi
    </h1>
  </div>
  <div class="box_content">
    <h3>Dodane</h3>
    <ul><%= render :partial => 'tag', :collection => @article.tags %></ul>
    <h3>Dostępne</h3>
    <ul><%= render :partial => 'tag', :collection => (@all_tags - @article.tags) %></ul>
  </div>
</div>

