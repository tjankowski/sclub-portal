<% if logged_in? %>
  <% if current_user.has_role?('administrator')  %>
    <div class="box">
      <div class="box_content">
        <% link_to new_match_path do %>
          <div class="inline_links">
            <%= image_tag "add.png", :size => "15x15", :hspace => "5" %>
            Nowy
          </div>
        <% end %>
      </div>
    </div>
  <% end %>
<% end %>

<div class="box">
  <div class="box_header">
    <h1>
      Mecze
    </h1>
  </div>
  <div class="box_content">
    <%= render :partial => "match_short", :collection => @matches %>
  </div>
</div>