<% if logged_in? %>
  <% if current_user.has_role?('administrator')  %>
    <div class="box">
      <div class="box_content">
        <% link_to :controller => "tags", :action => "new" do %>
          <div class="inline_links">
            <%= image_tag "add.png", :size => "15x15", :hspace => "5" %>
            <small>
              Nowy
            </small>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>
<% end %>

<div class="box">
  <div class="box_header">
    <h1>
      Tagi
    </h1>
  </div>
  <div class="box_content">
    <% for @tag in @tags  %>
      <%=h @tag.name %>
      <span class="link">
        <%= link_to "[Usuń]", :controller => "tags", :action => "delete", :id => @tag %>
      </span>
      <br />
    <% end %>
  </div>
</div>