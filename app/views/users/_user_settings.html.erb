<% if logged_in? %>
  <% if user == current_user  %>
    <div class="box">
      <div class="box_content">
        <% link_to edit_user_path(current_user) do %>
          <div class="inline_links">
            <%= image_tag "applications.png", :size => "15x15", :hspace => "5" %>
            Ustawienia konta
          </div>
        <% end %>
      </div>
    </div>
  <% end %>
<% end %>