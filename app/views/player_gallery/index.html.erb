<% if logged_in? %>
  <% if current_user.has_role?('administrator')  %>
    <div class="box">
      <div class="box_content">
      <% link_to new_gallery_path do %>
        <div class="inline_links">
          <%= image_tag "images.png", :size => "15x15", :hspace => "5" %>
          <span class="small_font">
            Nowa
          </span>
        </div>
      <% end %>
      </div>
    </div>
  <% end %>
<% end %>
<div class="box">
  <div class="box_header">
    <h1>
      Galerie
    </h1>
  </div>
  <div class="box_content">
    <h3>Dodane</h3>
    <ul>
      <%= render :partial => 'gallery', :collection => @player.galleries %>
    </ul>
    <h3>Dostępne</h3>
    <ul>
      <%= render :partial => 'gallery', :collection => (@all_galleries - @player.galleries) %>
    </ul>
  </div>
</div>